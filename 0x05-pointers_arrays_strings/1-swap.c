#include "main.h"

/**
 * swap_int - swaps two integers
 * @a: frist pointer
 * @b: second pointer
 * Return: Always 0
 */
void swap_int(int *a, int *b)
{
	int = temp;

	temp = *a;
	*a = *b;
	*b = temp;
}
