#include "main.h"

/**
 * _strlen - gives the length of astring
 * @s: pointer string
 * Return: Always integer
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
