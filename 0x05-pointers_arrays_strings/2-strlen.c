#include "main.h"

/**
 * _strlen - Counts the length of a string
 * @s: pointer to character
 *
 * Return: int
 */
int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		continue;
		i++;
	}
	return (i);
}
