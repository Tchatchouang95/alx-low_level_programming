#include "main.h"

/**
 * factorial - computes the factorial of a number
 * @n: input integer
 * Return: Always integer
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if
	{
		return (1);
	}
	return (n * factorial(n - 1));
}
