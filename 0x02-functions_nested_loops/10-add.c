#include "main.h"
#include <stdio.h>

/**
 * add - function to give the sum of two numbers
 * @x: 1st variable
 * @y: 2nd variable
 * Description: compute the sum of two integers
 * Return: the result
 */
int add(int x, int y)
{
	int result;

	result = x + y;
	return (result);
}
