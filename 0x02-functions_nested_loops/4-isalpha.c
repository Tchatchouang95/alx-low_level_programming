#include "main.h"

/**
 * _isalpha - Returns 1 or 0 depending on whether it alpha or not
 *
 * @c: placeholder
 * Description: tests alphabets
 * Return: Always 0
 */
int _isalpha(int c)
{
	return (c >= 'A' && c >= 'a' && c <= 'Z' && c <= 'z')
}
