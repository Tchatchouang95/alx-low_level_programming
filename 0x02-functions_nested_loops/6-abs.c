#include "main.h"
#include <stdlib.h>

/**
 * _abs - Computes the absolute value of an integer
 * @n: Input parameter
 *
 * Return: Always 0 Sucess
 */
int _abs(int i)
{
	int a;

	a = abs(i);
	return (a);
}
