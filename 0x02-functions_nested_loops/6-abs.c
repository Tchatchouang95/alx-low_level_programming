#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value of a given number
 *
 * Description: Every output is always positive
 * Return: Always 0
 */
int _abs(int n)
{
	return (abs(n));
}
