#include "main.h"

/**
 * _islower - determine if a letter is lowercase
 * @c: test parameter
 *
 * Description: checks for lowercase
 * Return: Always 0
 */
int  _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
