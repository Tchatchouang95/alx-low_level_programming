#include<stdio.h>

/**
 * main - main function
 * @argc: counter
 * @argv: pointer to an array holding string values entered on the terminal
 * Return: Always 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
